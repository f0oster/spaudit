{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Server (package debug)",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "${workspaceFolder}/cmd/server",
            "preLaunchTask": "Mage: Gen"
        }
    ]
}