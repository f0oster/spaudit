version: "2"
sql:
  - engine: "sqlite"
    schema: "database/migrations"
    queries: "database/queries"
    gen:
      go:
        out: "gen/db"
        package: "db"
        emit_interface: true
        emit_json_tags: true
        # overrides:
        #   - db_type: "TEXT"
        #     go_type:
        #       type: "string"
